import styled from '@emotion/styled';

const Container = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
`;

const DefaultLoading = () => <Container>로딩</Container>;

export default DefaultLoading;
